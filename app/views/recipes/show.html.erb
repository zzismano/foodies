<% content_for :meta_title, "Try this delicious #{@recipe.title} by #{@recipe.user.username} on #{DEFAULT_META["meta_product_name"]}" %>  <% content_for :meta_description, @recipe.caption %>  <% content_for :meta_image, cl_image_path(@recipe.photo.path) %>

<div class="container">
  <div class="meals">
    <%= render "shared/recipe_card", recipe: @recipe, comment: @comment %>
  </div>
</div>
