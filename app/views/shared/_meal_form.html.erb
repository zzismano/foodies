<%= simple_form_for([current_user, @meal]) do |f| %>
  <%= f.input :photo, as: :file %>
  <%= f.input :title %>
  <%= f.input :caption %>
  <%= f.input :restaurant %>
  <%= f.input :address %>
  <%= f.submit %>
<% end %>
