<%# <% content_for :meta_title, "Check out this delicious #{@meal.title} from #{@meal.restaurant} on #{DEFAULT_META["meta_product_name"]}" %>
<%# <% content_for :meta_description, @meal.caption %>
<%# <% content_for :meta_image, cl_image_path(@meal.photo.path) %>

<div class="container">
  <div class="meals">
    <%= render "shared/meal_card", meal: @meal, comment: @comment %>
  </div>

  <div class="rounded-5 shadow-lg"
    style="width: 100%; height: 600px;"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>

</div>
